class: Product
table: product
fields:
  productId: String, unique
  name: String
  image: String
  description: String
  price: double
  cartQty: int?, database
indexes:
  product_id_idx:
    fields: productId
