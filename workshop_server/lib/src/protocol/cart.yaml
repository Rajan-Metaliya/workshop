class: Cart
table: cart
fields:
  cartId: String, unique
  userId: String
  productId: String
  productName: String
  quantity: int
  productAmount: double
  totalAmount: double
indexes:
  cart_id_idx:
    fields: cartId
